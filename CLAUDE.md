# Dora 專屬工作區

## 使用者基本資訊
- **姓名**：朱加瑜（朱兒）
- **職業**：廣告行銷經理
- **居住地**：台中市

## 專業領域
- 數位行銷、Meta 與 Google 廣告投放
- ROAS 分析、受眾定位、轉換追蹤
- 素材分析、品牌市場調查

## 語言偏好
- 所有回應使用**繁體中文**
- 程式碼中的變數名稱、註解視需求使用英文

## 回應風格
- 視情況而定：簡單問題給直接簡短的答案，複雜任務附上脈絡與說明
- 不要在回應末尾加贅述性的總結（如「以上是我的回答」）
- 使用條列式或分段排版讓內容易讀

## 主要用途
此工作區用於：
- 寫作與內容創作（文章、創作、文案）
- 個人助理與規劃（任務管理、行程、備忘）
- 學習與研究（整理知識、分析資料）

## 一般原則
- 優先完成任務，必要時才詢問確認
- 不主動添加超出需求的功能或內容
- 除非特別要求，不加 emoji

## 執行權限設定

> 以下封鎖與允許規則已於 2026-07-06 落地到 `.claude/settings.local.json` 的 permissions（deny/allow），由系統強制執行，不再只靠文字宣告。修改時兩邊要同步。

### 預設模式
- 編輯檔案：自動允許
- 其他工具：逐次確認

### 永久封鎖指令
- 刪除檔案：`rm -rf`、`rm -r`、`rm -f`、`rm -fr`、`rm -R`
- 危險 Git：`git push --force`、`git push -f`、`git reset --hard`、`git clean -f`、`git branch -D`
- 系統操作：`sudo`、`reboot`、`shutdown`、`diskutil erase`、`mkfs`、`dd`
- 危險權限：`chmod 777`、`chmod -R 777`
- 覆寫檔案：`truncate`、`: >*`

### 本專案自動允許指令
- `export PATH=...`（設定環境變數）
- `npm --version`
- `curl *`（API 呼叫，包含 LINE 推播）
- `pbcopy *`（複製到剪貼簿）

---

## MCP 工具

### 本地安裝
Node.js 透過 nvm 安裝，路徑：`/Users/angela/.nvm/versions/node/v24.15.0/`

**Firecrawl**
- 用途：輸入網址取得乾淨的 Markdown 文字，適合爬網頁文章、社群貼文
- API Key 存於 `~/.claude.json` 的 env 設定中
- 帳號：firecrawl.dev（zhuer0323@gmail.com）

**Playwright**
- 用途：操控瀏覽器（點擊、截圖、填表單），適合需要登入或動態渲染的網頁
- 不需要 API Key，全域安裝於 nvm node 路徑下

**ffmpeg / ffprobe**
- 版本 8.1.2 靜態執行檔（Apple Silicon），裝於 `~/.local/bin`（無 Homebrew，來源 ffmpeg.martin-riedl.de）
- PATH 已設定於 `~/.zshrc`
- 供 `100_Todo/projects/video-autopilot-kit/`（短影音自動化）影片後製使用

**uv / mlx-whisper**（2026-07-15 裝）
- uv 裝於 `~/.local/bin`（curl 安裝腳本，不動系統設定）；mlx-whisper 以 `uv tool install` 安裝
- 辨識模型 whisper-large-v3-turbo（約 1.6GB，快取於 `~/.cache/huggingface`，下載過後離線可用）
- 供 `/會議分析` skill 本機語音轉文字（腳本 `scripts/meeting-transcribe.sh`；錄音不上傳網路）

### claude.ai 整合工具
- **Gmail**：搜尋信件、讀取內容、建立草稿、管理標籤
- **Notion**：搜尋/讀取/建立/編輯頁面與資料庫、管理留言
- **Canva**：搜尋/生成/編輯/匯出設計、管理資產與資料夾、協作留言
- **Google Drive**：目前僅支援授權連線驗證

### 注意事項
- 若 MCP 連線失敗，確認 nvm node 路徑是否存在
- 設定存於 `~/.claude.json` 的 Dora專屬 專案區塊

---

## 外部安裝的 Skill

> 自己建的 skill 放在 `000_Agent/skills/`（已 symlink 至 `~/.claude/skills`）；
> 從別人的 repo 裝來的也放同一層，所以會跟著 OneDrive 一起同步到其他機器。

### speak-human-tw（2026-08-13 裝）
- **來源**：`git clone https://github.com/Raymondhou0917/speak-human-tw.git ~/.claude/skills/speak-human-tw`（作者 Raymond Hou，MIT，目前 v1.4.0）
- **用途**：繁體中文**去 AI 味**改寫 — 抓 AI 痕跡、校正中國用語與半形標點
- **觸發**：`/speak-human-tw`，或說「這段好 AI」「去 AI 味」「校對一下再發」
- **行為**：會先列「原句／為什麼要改／建議改法」編號清單讓朱兒勾選，確認後才動筆，不直接覆蓋原檔
- **跟寫作類 skill 的分工**：`社群文案撰寫`、`廣告文案撰寫`、`報價單` 負責產出；這個只負責事後除味，**不會加個人語氣**（要語氣還是走 `200_Reference/writing-samples/`）
- **更新**：`git -C ~/.claude/skills/speak-human-tw pull`
- **不進 dora-agent 版控**：它自己帶 `.git`，已加進 Dora-Agent 的 `.gitignore`；換機時照上面的 clone 指令重裝即可（OneDrive 檔案本身還是會同步）

---

## LINE 小助理（Dora賺錢小能手）

- **Bot 名稱**：Dora賺錢小能手（@462xyoib）
- **用途**：從 Claude 推播訊息到朱兒的 LINE
- **憑證**：`LINE_PUSH_TOKEN`、`LINE_USER_ID` 存於 `~/.claude/settings.json` env 區塊
- **推播方式**：內容寫入 `/tmp/dora_*.txt` → 執行共用腳本 `scripts/dora-line-push.sh <文字檔>`（morning 與廣告回報 skill 均使用；改 token 或格式只改這一處）

### 8:30 自動早報（週一～週五）
- **腳本**：`~/Library/Scripts/dora-morning-briefing.sh`
- **憑證**：`~/Library/Scripts/dora.env`（LINE token ＋ 工作台連線設定，權限 600）
- **排程**：`~/Library/LaunchAgents/com.dora.morning-briefing.plist`（已啟用，週一～週五 8:30，假日不觸發）
- **內容**：櫻花粉 Flex Message 卡片，包含：
  - ♈ 牡羊座今日運勢（從 astro.click108.com.tw 即時抓取）
  - 幸運數字、幸運色、方位
  - 📋 今日待辦（**來源＝工作台，2026-08-17 起不再讀 Notion**）
  - 每日輪替加油語
- **卡片文字一律靠左**（2026-08-17 改；原本標題、運勢、幸運速查都是置中，現在整張統一 `align:start`，
  待辦區沿用 Flex 預設不另外寫）
- **Mac 要開機才會觸發**
- **注意**：腳本與憑證必須放在 `~/Library/Scripts/`，放 `~/Downloads/` 會因 macOS TCC 權限被擋

### 早報的待辦怎麼來（2026-08-17 改）
- **資料來源**：工作台的 Firebase（`ws_k7m2q9xr4t/tasks` ＋ `clients`），**只讀不寫**
- **憑證**：服務帳號金鑰 `~/Library/Scripts/dora-workspace-sa.json`（權限 600，等同後門鑰匙，
  絕不進版控；`.gitignore` 已擋 `*firebase-adminsdk*.json`）。
  重生金鑰走 Firebase 控制台 → 專案設定 → 服務帳戶 → 產生新的私密金鑰
- **怎麼拿 token**：腳本自己簽 JWT 換 OAuth token，**RSA 簽章交給系統 `openssl`**，
  不裝 `cryptography`／`PyJWT`／`google-auth`（系統 python3 是 3.9.6，什麼套件都沒有，
  launchd 環境也不吃 venv）
- **篩選規則**：分「❗拖到的」（到期日 < 今天且未完成）與「📌今天要做的」（到期日 = 今天），
  各自照重要度排、各最多列 6 條、超過顯示「還有 N 件」；**每日例行不列**（每天都出現會洗版）；
  每條前面帶客戶簡稱 `[短名]`（任務存的是客戶 id，靠 `clients` 對照，對不到就只列任務名）
- **讀不到時**：待辦區顯示「暫時讀不到工作台資料」，運勢與加油語照常推，不會整則不見
- Notion 的 `NOTION_TOKEN` / `NOTION_TASKS_DB` 仍留在 `dora.env` 與 `~/.claude/settings.json`，
  但**早報已經不用它們**（其他地方要用再說）

### /morning skill（AI 版早報，需在 Claude 內觸發）
- 唯一的 AI 早報入口（舊的 `.claude/commands/morning.md` 已於 2026-07-06 併入此 skill）
- 讀取 Notion 計畫資料庫今日待辦 + 專案管理進行中專案 + 本地 plans/daily log
- 產出個人化早報並推播到 LINE

### 廣告回報推播
- 用 `/廣告回報` skill 產出回報後，自動推播到 LINE 並複製剪貼簿
  （2026-08-19 從「廣告週報」改名，因為做的是走期累積的回報、不一定以週為單位）

### 週三 9:00 週報提醒（2026-08-19 上線）
- **腳本**：`~/Library/Scripts/dora-weekly-reminder.sh`（repo 備份在 `scripts/`）
- **排程**：`~/Library/LaunchAgents/com.dora.weekly-reminder.plist`（每週三 9:00，已啟用）
- **名單來源**：工作台客戶的「每週三要做週報」開關（`wk`），沒有任何一家打開就**不推**
- **憑證**：沿用 `dora.env` 的 `WS_*` 與 LINE token；腳本必須放 `~/Library/Scripts/`（TCC）

### LINE 小秘書（2026-08-17 上線）：在 LINE 打字就排進工作台
同一隻機器人現在是**雙向**的——早報是推出去，小秘書是收進來。

- **程式**：`100_Todo/projects/line-secretary/`（Cloudflare Workers，免費方案，詳見該資料夾 README）
- **接線生網址**：<https://dora-line-secretary.zhuer0323.workers.dev>（已填進 LINE 的 webhook）
- **用法**：打「`8/20 漁三 結案報表`」→ 排進工作台 → 回一句確認（日期漏寫就排今天並註明）
- **這一版只做新增**：查詢、打勾完成、改內容都還沒做
- **兩道門檻**：驗 LINE 簽章 ＋ 只認朱兒的 userId（工作台有客戶名稱，兩道都不能拿掉）
- **機密存在 Cloudflare**（`wrangler secret`），程式碼與 repo 裡都沒有：
  `LINE_CHANNEL_SECRET`／`LINE_ACCESS_TOKEN`／`LINE_USER_ID`／`FIREBASE_SA`
- ⚠️ **分類規則是抄的，要手動同步**：`src/classify.js` 複製自工作台 `index.html`
  （`CLIENT_ALIASES`／`DEFAULT_TYPES`／`TYPE_PRIORITY`／`extractHead`／`normKey`）。
  改網頁那邊的規則時，這裡要一起改，不會自動同步
- **不自動建新客戶**：認不出客戶只在回覆裡提一句，由朱兒到工作台決定（免得 LINE 隨手打字長出假客戶）
- **不寫 `ui.learn`**：學習只走網頁那邊，小秘書只讀
- **Worker 跑在 UTC**：`date.js` 一律先加 8 小時算台灣時間
- **本機測 webhook 要帶 User-Agent**：不帶會被 Cloudflare 擋成 403，不是程式壞掉

---

## 記帳 App（2026-08-19 上線）

**個人生活開銷**，跟工作台分開的獨立 App。工作是工作、生活是生活，開起來也比較快。

- **網址**：<https://zhuer0323-tech.github.io/dora-workspace/money/>（可裝到手機桌面；工作台寶藏工具 👛 也有入口）
- **原始檔**：`100_Todo/projects/money/index.html`（單一檔案，push main 就自動部署）
  ＋ 自己的 `manifest.webmanifest`／`sw.js`／圖示（粉底白錢包，跟工作台的花圖示分得出來）
- **資料**：Firebase 同一個專案 `busan-trip-2026-201f8` → **`mn_x3f9b6qz`** 節點，
  規則一樣綁 `zhuer0323@gmail.com`；設定說明在 `100_Todo/plans/2026-08-19-記帳-Firebase設定.md`

### 內容
- **三個分頁**（底部），預設開在「記一筆」
  - **記一筆**：自製數字鍵盤（不叫系統鍵盤，快）、**支出／收入／轉帳**三選一、
    分類一排點一下、選帳戶、日期預設今天、備註選填。頂端一條細的預算進度條
  - **總覽**：最上面「目前有多少錢」（各帳戶餘額＋總計），下面 **週／月／年** 可切換的
    三張卡——還能花多少／錢花到哪去／存了多少；點分類橫條會跳出那一類的每一筆，
    點某一筆可直接開來改
  - **明細**：依日期分組，顯示帳戶名，點一筆可改可刪
- **設定**：每月預算、存錢目標、**帳戶管理**、分類增刪改名（**改名會一起更新已經記過的帳**）、
  掃紙本發票、登出

### 帳戶與轉帳（2026-08-19 加，朱兒指定分帳戶管）
- 帳戶三種類型：現金／銀行／信用卡，可自己加（`settings.accs`，每個有 `open` 起始餘額
  與 `openD` 起算日）
- **起始餘額＝填的那天當下的餘額**，那天之前的帳不再重複扣（`accBalances()` 用 `openD` 過濾）
- **信用卡用「餘額變負數」表示欠款**：刷卡 3,000 → 卡 −3,000；月底從銀行轉 3,000 過去
  → 卡回 0、銀行 −3,000。總資產自動扣掉卡債，不用另外寫一套
- **一定要有「轉帳」**：付卡費是把錢從銀行搬到卡，不是花錢（刷的當下已經記過支出）。
  沒有轉帳的話月支出會憑空多一倍。**轉帳一律不計入 out／inc 統計**（`sumOf()` 直接 return）
- 轉帳的資料是 `{kind:'tr', from, to}`，沒有 `cat`／`acc`；編輯視窗不給改帳戶（要改就刪掉重記）

### 週／月／年三種區間（2026-08-19 加）
- `period()` 回傳起訖、標題、該區間的預算、能不能往後翻；`shiftPeriod(n)` 前後翻
- **週一為一週的開始**（台灣習慣，`mondayOf()`）
- 週預算＝月預算 ÷ 當月天數 × 7；年檢視改成「今年花了多少」＋平均一個月，
  **分母只算有紀錄的月份**（`monthsCounted()`），不然一月開始記跟十二月才開始記會被當成一樣
- 明細頁維持只看月，沒有跟著切區間
- **分類預設**：支出 飲食／交通／日用／娛樂／醫療／人情／其他；收入 薪水／獎金／接案／其他。
  ⚠️ **朱兒說要自己列一組，2026-08-19 還沒給**；她給了就換掉 `DEF_OUT`，
  並同步 `line-secretary/src/money.js` 的 `ALIAS` 講法對照表。
  收入的顏色刻意往後挪 6 格，才不會跟支出撞色
- **沒訊號照記**：先寫進手機、排進 `dora_money_pending`，右上角顯示「待補 N 筆」，
  連上網或回到前景自動補寫。**寫入是逐筆寫**（不是整包覆蓋），多裝置同時用不會互相蓋掉

### 掃紙本發票（收在設定裡，不在主畫面）
朱兒的發票**多半走雲端載具**，手上不會有紙本，所以這顆刻意收起來。
- 解析台灣電子發票左邊那個 QR：發票號碼(10)＋開立日期(7，民國)＋隨機碼(4)＋銷售額(8，**十六進位**)
  ＋總計額(8，十六進位)，取總計額換成十進位當金額
- 用瀏覽器內建的 `BarcodeDetector`，**iPhone 的 Safari 不支援**，會直接提示改手動輸入
- **不做 AI 拍照辨識**（掃碼免費、離線、不會認錯字）

### 手機捷徑／LINE 記帳（2026-08-19 一起上線）
- 走 **LINE 小秘書那隻 Worker 的 `/money` 路徑**（沒有另外開一隻），
  詳見 `100_Todo/plans/2026-08-19-記帳-手機捷徑.md`
- 捷徑打「`120 午餐`」→ 直接寫進雲端 → 跳通知，**完全不用開 App**
- LINE 打「`記帳 120 午餐`」也可以；**開頭要有記帳字眼**才走記帳，其餘照舊排任務
- 密碼 `MONEY_TOKEN` 存在 Cloudflare，本機備份 `~/Library/Scripts/dora-money-token.txt`。
  ⚠️ **dora-workspace 是公開倉庫，這串絕對不能進版控**
- 分類清單讀雲端 `settings`，**不自動長出新分類**；講法對照表在 `money.js` 的 `ALIAS`
- 測試加 `?dry=1` 只回結果不寫入

### 維護重點
- **記帳跟工作台完全分開**：不同節點、不同檔案、不同 App，改記帳不會動到任務
- **記帳的日期往回找**，排任務的日期往未來找，兩套解析不共用（`parseSpentDate` vs `parseDue`）
- **還沒做**：從財政部載具明細匯入（她發票走載具，這是最乾淨的自動抓帳路線）、
  每月比較與趨勢、家庭共用、發票對獎

---

## 朱兒專屬工作台（2026-08-09 上線）

**任務管理的主場，已取代 Notion 排任務。**

- **網址**：<https://zhuer0323-tech.github.io/dora-workspace/>（可安裝成桌面／手機 App）
- **原始檔**：`100_Todo/projects/index.html`（單一檔案）；push main 就自動部署
- **資料**：Firebase `busan-trip-2026-201f8` → `ws_k7m2q9xr4t` 節點
- **權限**：規則綁定 `zhuer0323@gmail.com`，沒登入讀不到任何一筆
  （因為任務裡有客戶名稱；設定說明在 `100_Todo/plans/2026-08-08-工作台-Firebase設定.md`）

### 內容
> 分頁名稱：今日代辦／工作規劃／寶藏工具／專案客戶／復盤／設定
> （程式裡的 `data-tab` 仍是 today／tasks／tools／clients／review／settings，只有顯示文字換掉）

- **今日代辦**：時鐘、今日聚焦、快速記一筆、**今日時間表**、每日例行、今日概覽、需要注意
  （時鐘卡另有農曆日期與每日一句：農曆走瀏覽器內建的 `Intl` 中國曆，不維護萬年曆表也不用連網，
  **只顯示月與日**，年份干支生肖講的是今年、不是朱兒本人，會看混所以拿掉；
  每日一句照一年的第幾天輪，語氣沿用 8:30 早報的加油語）
  七張卡可拖曳排序、標題可點擊改名，順序與名稱都同步雲端
  （今日時間表＝日規劃整排寬的卡片，跟任務頁的「日」檢視共用 `dayGrid()`，
  差別是它固定看今天、不吃任務頁的篩選、列高壓低；一樣可以拖卡片排時段）
  （快速記一筆是**兩段式**：第一次 Enter／按「加入」只展開確認區，可改到期日、重要度、狀態、備註，
  再按一次 Enter 或「確定加入」才寫進去，Esc 收起來；客戶與類型的自動判斷維持原樣）
- **工作規劃**：連動式儀表板 ＋ 清單／日／週／月／年 五種檢視，共用同一組篩選
- **寶藏工具**：禾言社群規劃、報價單、會議報告、IG 腳本、釜山旅遊、數學遊戲
  （2026-08-17 重排：工作常用的往前，禾言社群規劃放第一顆）
- **專案客戶**：分**廣告投放／顧問陪跑／專案協作／合作廠商／LF**五區（客戶的 `group` 欄位，
  沒有這欄的舊資料一律當廣告投放，不需要搬移），每區是一排 4–5 個的小卡
  （手機 2 欄→640px 3 欄→980px 4 欄→1240px 5 欄）；已結案的另外收在最下面一區、不能拖。
  卡片上顯示的是客戶的 `duty`（**我負責的項目**，朱兒自己在編輯視窗打，可換行，卡片最多顯示三行），
  **不是**系統算的未完成件數；沒填會顯示灰字提示。
  編輯視窗另有**專案走期**（2026-08-19 改成一期一列：期數＋起訖日，
  按「＋新增一期」自動帶下一個期數、起日接上一期結束的隔天；底下標「週報會用第 N 期」，
  抓的是含今天的那一期，都不含就用最後一期。存在客戶的 `runs` 陣列，
  舊的單一字串 `run` 開啟時自動轉成第一期、原值保留）、
  **廣告專案資訊**（廣告格式／素材類型／廣告預算）與
  **「每週三要做週報」開關**（2026-08-19 加）：前者是週報分析與建議的依據，
  後者是週三提醒的名單來源，**兩者都只在編輯視窗裡，不顯示在卡片上**。
  週報那邊靠 `scripts/dora-ws-clients.py` 讀（唯讀，走早報同一把服務帳號金鑰）。
  可自行新增／改簡稱／設認字關鍵字，每個可掛 Google 雲端資料夾連結。
  按住卡片右上角 ⠿ 可拖（`makeClientSortable()`）：同區換順序存 `order`，拖到別區改 `group`。
  卡片左邊的 logo 存在客戶的 `logo` 欄（data URI）：上傳時在瀏覽器縮成 96×96 白底方塊，
  **整張縮進去不裁切**（客戶 logo 多半是橫的），webp 優先、Safari 舊版退回 jpeg，一張約 2–10KB；
  沒上傳就顯示簡稱第一個字，底色照 id 固定
- **復盤**（2026-08-17 上線）：一週一次回頭看，資料全部從既有任務算，**不用填任何東西**。
  四張卡：這週完成了多少（大數字＋跟前一週比＋每日例行完成率）、什麼事一直在拖、
  時間花在哪個客戶、做的是哪些類型的事。上方可往前翻週次（不能看未來）。
  **點客戶或類型的橫條會跳出視窗列出那幾件是什麼**（日期＋名稱＋另一個維度當標籤），
  點清單裡任一件可直接開那筆任務
- **設定**：匯入 Notion、工作類型管理、自動分類、匯出備份、還原、重設版面

### 自動分類（2026-08-10 上線）
打任務標題就自動判斷客戶與工作類型，判斷結果顯示在輸入框下面，可當場改。
- **判斷順序**：學過的對應 → 關鍵字表 → 標題開頭那段字當新客戶
- **關鍵字表**：客戶＝全名＋簡稱＋`CLIENT_ALIASES`（從 `notion-to-workspace.py` 搬進網頁的別名）
  ＋客戶頁自己填的；類型＝設定頁「工作類型」每一列的關鍵字。一律最長的關鍵字優先，「內部」永遠最後比對
- **會學**：手動改掉猜測結果 → 記進 `ui.learn`，下次同樣的字直接用她的答案；設定頁可單筆刪或全清
- **認不出客戶**：取標題開頭到第一個動作詞之前那段字自動建客戶，標「待確認」置頂；
  超過 8 個字或找不到動作詞就留空不建
- **狀態四色**：待開始黃、進行中綠、已完成藍，**日期已過又沒完成一律紅**（紅蓋過前三色）
- **工作類型 10 種**：廣告（含上線／名單／調整／走期／社群／限動／文案／素材）、會議、提案、
  **禾言**（提到禾言、刷卡明細、雲端更新／調整／整理）、內部（只要提到「檢查」）、結案報告、
  報價單、KOL（kol／網紅／部落客）、週報回報（含「廣告回報」）、其他
- **比對規則**：類型用**整串標題**比對（不能先切掉客戶名，「禾言」「刷卡明細」同時是客戶別名與類型關鍵字）；
  關鍵字愈長愈優先；`TYPE_PRIORITY = ['禾言','內部']` 這兩類整組最優先（照名稱硬編，改名就失效）
- **舊類型自動併過去**：`migrateTypes()` 依 `ui.typesVersion` 分版跑，每版只跑一次
  （v2 併成 9 種／v3 改 KOL 關鍵字／v4 加禾言並回頭搬），原本的類型留在任務的 `typeOld` 欄

### 看板拖曳（2026-08-10）
按住卡片上的 ⠿ 就能拖，日檢視與週檢視都支援：
- 「還沒排時間」區裡換前後順序 → 存任務的 `ord`
- 往下拖到時間軸的某一格 → 設定 `time` 為該格整點；**同一時段要放幾件都可以**（`min-height` 不設上限）
- 從時間軸拖回「還沒排時間」→ 清掉 `time`
- 週檢視拖到別天 → 順便改 `due`（重複任務不動）
- 拖曳中目標格會亮（`.dropok`），靠近視窗上下緣會自動捲動
- 「還沒排時間」是多欄網格（手機 1 欄→520px 2 欄→1000px 3 欄→1300px 4 欄），
  所以換位置時要看**左右**中線；單欄的時間格與週檢視看上下（`makeCardSortable` 裡用
  `gridTemplateColumns` 判斷）

### 復盤的算法（2026-08-17）
- **完成時間分兩套，不能混**：一般任務看 `doneAt`、每日例行看 `doneDates` 的日期 key。
  例行的打勾**不併進完成件數**（每天打勾會把客戶統計灌爆），單獨算成完成率
- **沒有 `doneAt` 的舊任務一律不算**，不拿到期日硬湊；改在頁尾誠實寫「有 N 筆沒列入」
- **例行完成率**只算到今天為止（本週還沒過完的日子不列入應做次數），
  而且任務建立日之前的日子不算它沒做
- **拖延清單講的是「現在」還沒完成的**，所以只在「本週」那頁出現，翻到過去週次就不顯示，
  免得被當成那一週的數字
- **客戶排行是件數不是工時**（工作台沒有碼錶）。一份結案報表跟一則限動都算一件，
  所以旁邊一定要放工作類型分布當對照
- 這一頁**只讀不寫**，唯一例外是點拖延清單或明細視窗裡的某一件去開任務（走既有的 `openTask()`）
- 橫條超過 8 名會併成一列，那列叫 **「其餘 N 項」不叫「其他」**——
  工作類型本身就有一個叫「其他」的，同名會撞（合併列靠 `data-rvrest` 認，不是靠名字）
- 明細視窗（`#rvModal`）沿用既有的 `.modal`／`.sheet`（手機從底部滑上來、桌機置中）；
  點清單裡的某一件會**先收起這個視窗再開任務**，兩層視窗才不會疊在一起
- 任務的標題欄位是 **`name` 不是 `title`**（2026-08-17 踩過，寫成 title 會整排顯示「沒有標題」）
- 樣式沿用統計頁既有的 `.dbox`／`.drank`／`.drow`／`.dempty`，沒有另外做一套
- **版面（2026-08-17 她回報「看上一週版會亂掉」後修的）**：本週是 2×2 四張卡；
  過去週次只剩三張，兩欄排會落單，所以「這週完成了多少」那張改成 `grid-column:1/-1` 整排寬。
  卡片**同一排等高對齊**（跟今日代辦頁同一套規則），底部說明用 `margin-top:auto` 貼齊下緣——
  所以那幾行 `.tiny` **不能再寫行內的 `margin-top`**，會蓋掉 auto

### 版面原則（2026-08-11 整理過）
- 今日代辦同一排的卡片**等高對齊**（`#board{align-items:stretch}` ＋ `.tile>.card{height:100%}`）
- 「還沒排時間」是多欄網格，十幾筆才不會把頁面撐得很長；`.tile.wide` 是整排寬的卡片
- 星座運勢**做不到即時**：網頁抓 astro.click108 會被跨網域擋掉，
  要等 8:30 早報改讀工作台（需 Firebase 服務帳號金鑰）才有辦法把運勢寫進雲端給網頁讀

### 維護重點
- **新增客戶**：直接打字讓它自動建，或客戶頁手動新增；匯入路線仍看 `scripts/notion-to-workspace.py` 的 `CLIENT_KEYWORDS`
- **新增工作類型**：設定頁「工作類型」自己加，改名會一併更新已經用到的任務
- **重跑匯入是安全的**：已匯過的不會變兩筆，只補原本空著的客戶欄，不會蓋掉後來改的內容
- **設定頁「用新規則補一次分類」**：只填空的客戶欄與還是「其他」的類型欄，不覆蓋、不建新客戶
- **時間軸固定 8:00–19:00**（朱兒上班時段）；沒填預計時段的任務放在每天上方的「未排時間」區
- **8:30 早報已經改讀這裡的任務**（2026-08-17 完成，走服務帳號金鑰唯讀，設定寫在上面「早報的待辦怎麼來」）
- **任務視窗的自動儲存有「倒數」，改動它要小心**（2026-08-18 修過一次）：
  既有任務是停手 500ms 才寫入。關視窗（儲存／取消／叉叉／點背景）一律先 `flushAutoSave()`
  ——**存完再關，不是丟掉**；倒數醒來時還要再確認 `editingTask.id` 是不是同一筆。
  **只有刪除那條路是 `clearTimeout` 丟棄**，補存會把剛刪的寫回來。
  少任何一道，改完馬上關視窗就會多跑出一筆（舊的沒吃到修改＋改過的變新的一筆），
  因為 `readTaskForm()` 找不到 `editingTask` 就會自己發一個新 id
- **還沒做**：甘特圖、個人區（只有佔位頁）、把運勢與每日一句寫回工作台變成網頁上的卡片
  （金鑰已備好，要做就是加寫入那一段）；
  復盤區之後可加的：心得／反思填寫區、月復盤、連續幾週的趨勢折線（等資料累積 5–6 週再做才有意義）

---

## 禾言社群規劃網頁（2026-08-13 上線）

**禾言自家社群**（不是客戶的）的月度議題與文案看板。

- **網址**：<https://zhuer0323-tech.github.io/dora-workspace/heyen-social/>（工作台寶藏工具 🌾 也有入口）
- **原始檔**：`100_Todo/projects/heyen-social/index.html`（單一檔案，push main 就自動部署）
- **資料**：Firebase 同一個專案 `busan-trip-2026-201f8` → `hy_social_r7n3k8` 節點，
  規則一樣綁 `zhuer0323@gmail.com`（貼文裡有客戶名稱與案例數據）；
  設定說明在 `100_Todo/plans/2026-08-13-禾言社群規劃-Firebase設定.md`

### 內容
- 月曆／卡片／**圖文表**三種檢視，可切月份；點卡片開編輯視窗
- **月曆格子照她試算表的排法**：上面一行是議題（「社群(前台)」＋類型兩個標籤，**排同一行不換行**），
  下面是貼文主題；有填「標題」欄才多顯示一行灰字。
  「⋯」按鈕疊在卡片右上角不佔寬度；**視窗窄於 1180px 只留類型那顆**，
  免得被截成「社…」（她要橫式，所以寧可少一顆也不換行）
- **月曆上方有議題統計**（`renderCalStats()`）：總篇數、四類各幾篇、其他、已發布／未發布
- **素材編號自動算**（2026-08-17 上線）：`H` ＋ 該月英文第一個字母 ＋ 兩位順序號，
  順序照上線日期（同一天再看 `ord`）。8 月 August＝HA01…、9 月 September＝HS01…、10 月 October＝HO01…。
  **在月曆上把貼文拖到別的日期，編號就自己重算**，圖文表那格與編輯視窗都改成唯讀，不給手填。
  資料裡舊的 `code` 值留著沒刪，但介面一律顯示自動算的（`autoCode()`）。
  會撞的月份用兩碼（2026-08-17 定）：1月 HJA、3月 HMA、4月 HAP、5月 HMY、6月 HJU、7月 HJL；
  **8 月維持單碼 HA**（HA01–HA03 已發布也在她試算表裡，不能改）。十二個月的碼互不重複
- **圖文表**是照朱兒的 Google 試算表「禾言社群規劃」的圖文模板做的
  （<https://docs.google.com/spreadsheets/d/1BGuJUZs9IbZP0FWXuKyM4tZlMT8wPg8RDf-SSIxo_MQ/edit>）：
  一篇貼文一欄，左邊 13 個欄位名固定（`SHEET_ROWS`）—— 素材編號(⭐成效佳)／素材名稱／貼文類型／
  貼文主題／素材類型／上線時間／貼文連結／素材圖／廣告格式／文案編號／文案／標題／歡迎語常見問題。
  貼文類型那格跟她表單一樣是兩個 chip：「社群(前台)」＋四類其中之一。
  多出來的欄位在編輯視窗的「圖文表的其他欄位」收合區裡填
- 每篇存：日期、類型、標題、目的、貼文文案、已發布
- **內容類型四種**（2026-08-13 朱兒定的架構）＋「其他」兜底，各有代表色：
  **觀點/趨勢**（紫）、**教學/新手**（藍）、**日常/節慶**（橘）、**廣告顧問陪跑**（綠）
  程式裡 class 用英文 slug（`view`／`teach`／`life`／`coach`／`etc`），因為類型名稱有斜線
- 改過類型架構要走 `migrateTypes()`：照雲端 `meta.typesVersion` 分版跑，每版只跑一次，
  跑完寫回版本號，之後她自己改類型才不會又被搬回去（v2＝舊的廣告知識／AI趨勢／操作方法／節慶搬過來）
- 文案有「複製」鈕；勾已發布卡片變淡、標題劃線
- **點一下就能改**：圖文表每一格點下去變輸入框（文案是多行，Cmd/Ctrl+Enter 存、Esc 取消）；
  月曆點標題直接改，要開完整編輯按卡片右邊的「⋯」。編輯中不重畫（`editingCell` 旗標），
  免得雲端同步把正在打的字洗掉
- **可以拖**：月曆的貼文拖到別的日期就改上線時間；圖文表拖「素材編號」那格換整欄位置（存 `ord`）。
  `monthPosts()` 排序是 ord 優先、沒有 ord 才照日期
- **文案收尾**（2026-08-13 定）：每篇結尾都要有 **【禾言觀點】或【禾言怎麼做】** 一段，
  接一句互動提問或行動呼籲，最後才是署名與 hashtag。格式照她 Google 試算表 8 月那批。
  節慶篇不用觀點，用行動呼籲就好。
  改 SEED 的文案後要把 `CONTENT_VERSION` 加一，`migrateContent()` 才會把雲端的文案補上去（只跑一次）。
  **v3 起改用「標題」比對而不是日期**（日期會被拖來拖去，用日期會把文案蓋到別篇上），
  同時會更新日期與類型、把雲端漏掉的篇補回來（標題前 8 字相同就當同一篇，不會補成重複）
- **只有一欄文案**（2026-08-13 朱兒要求）：原本分 IG 版與 FB 短版，
  現在只留完整版那一份，存在資料的 `ig` 欄。舊的 `fb` 短版值還留在資料裡但介面不顯示，
  存檔時原值帶過去不會被清掉；`SEED` 裡也還有 `fb`，只是不會再用到
- 雲端第一次是空的才會種入 9 月那 9 篇（`SEED`），之後都讀雲端

### 社群定位（2026-08-13 定）
IG 為主 FB 轉發；**一週一篇，固定週二**（節慶當天可加開，例如 9/25 中秋）；
受眾是中小企業老闆與行銷窗口；
目的是**帶詢價 ＋ 建立專業形象**。內容走四線：觀點/趨勢、教學/新手、日常/節慶、廣告顧問陪跑。
**案例先不寫**（朱兒說之後再加），語氣用**活潑版**（emoji 與畫面感，接近她幫客戶寫的風格）。
2026-08-13 把原本 9 月那 10 篇拆成兩個月（她說一週一篇就好）：
- **9 月 6 篇**：教學 3、觀點 1、節慶 1、陪跑 1
- **10 月 4 篇**：教學 2、觀點 1、陪跑 1

排新的月份照這個比例走：**每個月至少一篇陪跑**（那條線最直接帶詢價）、
教學不要超過一半、有節慶就加開一篇。有時效的議題（檔期、節慶）不要往後搬。

**排新議題前先查有沒有寫過**（2026-08-14 教訓：「報表先看這三個數字」跟她 7／8 月既有內容撞題，整篇拿掉）。
查的地方：她的 Google 試算表各月圖文分頁 ＋ `100_Todo/drafts/social-posts/` 既有月檔。

**排序鐵則（2026-08-13 朱兒交代）：不同議題要交錯開，不要同一類連著發。**
教學多的月份，把教學放在單數位（第 1、3、5 篇），中間夾觀點與陪跑。
排完自己檢查一次相鄰兩篇的類型，一樣就換位置；
但**有時效的篇不能為了交錯亂搬**（例如講中秋檔期的要在中秋前）。

### 素材來源
- 品牌檔：`200_Reference/clients/禾言數位行銷.md` — 核心價值、五大服務、19 個案例含實際數據，
  從對外簡報 `【禾言】電商門市行銷廣告精選案例.pptx` 與六份報價單整理出來的
- 文案存檔：`100_Todo/drafts/social-posts/YYYY-MM_禾言社群文案.md`
- **她原本的工作表**：Google 試算表「禾言社群規劃」（月規劃＝月曆排程＋篇數統計、月圖文＝圖文表、模板兩份）。
  8 月三篇 HA01–HA03 已經搬進網頁（8/13、8/19、8/26，標記已發布）。
  要抓試算表內容：Google Sheets 是 canvas 渲染 DOM 抓不到，**用名稱方塊選範圍 → Cmd+C → `pbpaste`**
- **文風兩種都有**：HA01 是活潑版（📢🌟 條列＋「立即私訊禾言小編」），HA03 是知識版（【GEO 是什麼】【禾言觀點】）
- **待朱兒補**：品牌語氣定調、團隊要不要露臉、現有 IG 帳號、禁用詞（列在品牌檔最下面）

### 做成圖卡：`禾言圖文` skill（2026-08-14 建）
規劃表 → **先給朱兒確認議題與文案** → 出 6 頁 IG 圖卡（1080×1350）。

**版型 2026-08-17 定案：柔和版**（無框大圓角）。`V1改_`（粗黑框）、`final_`（盾牌形）不再維護。
模板原始檔 `100_Todo/drafts/social-cards/柔和版v2_原始檔.html`。

**最終產出是 Canva 可編輯檔，不是 PNG**（2026-08-17 朱兒要求）：
HTML 覆蓋到 `100_Todo/projects/heyen-cards/index.html` → push → 等 Pages 更新 →
`import-design-from-url` 匯 <https://zhuer0323-tech.github.io/dora-workspace/heyen-cards/> →
`read-design` 看縮圖驗收 → 把 `edit_url` 給她。每張卡要有 `data-document-role="page"`。

**Canva 匯入鐵則**（2026-08-17 連匯五輪測出來，寫在 `禾言圖文` skill 裡）：
- 夾在文字行中間的**純色背景色塊會走樣**（黑底白字變黑槓、膠囊被壓成兩行小字）→
  強調一律用 `linear-gradient` 畫；黃紙頁不靠底色，改深紅字 `#C7302F`
- 圖片**一定要絕對網址**，相對路徑的 logo 匯入後整排消失
- **字型不用處理**：匯入時 Canva 套的中文字型就是源柔（2026-08-17 確認，fontRef 前後一致），
  不用提醒她換；真的不對時只能她自己走「設計 → 樣式 → 字體」，程式改不了字型
- **不放頁碼**（2026-08-17 朱兒要求拿掉右下角頁碼）
- 純文字顏色、圓角、陰影、SVG、格線背景都正常

**規劃表有「複製這篇給 Claude」按鈕**（圖文表文案格、編輯視窗各一顆）：
按下去把日期／類型／議題／目的／文案整理成一段，貼過來就能直接做圖。

**示意圖與版型細節**：
- logo 用 `heyen-cards/logo_use.png`
- 為什麼版面在 HTML 排而不是在 Canva 裡做：Canva MCP 不能搜素材庫、只吃點陣圖、
  裁切又不能縮放，換圖幾乎做不到；HTML 這邊示意圖全部用 SVG 自己畫，改字幾秒就好
  （Canva 只當最後的可編輯交付格式）
- **示意圖要依內容設計**（她特別交代）：對比型用左右對比卡、階段型用漏斗（highlight 當頁那層）、
  並列型用編號列表卡、總覽用完整漏斗。不是套同一個模板
- 出圖：headless Chrome 截整頁 1080×8100 再用 PIL 切六張（Playwright 元素截圖會逾時，別用）

**舊的 Canva 路線（已停用，資料留著備查）**：
- 模板：`DAHQAQySegM`「廣告上線前，到底經過多少雙眼睛？」（只讀不寫，每次複製新的一份）
- 文案來源以**雲端為準**（她會在網頁上直接改），讀不到才退回 `100_Todo/drafts/social-posts/`
- 第 5 頁底部那格固定放【禾言觀點】
- 做完把 Canva 連結填回規劃表那篇的「貼文連結」欄
- **Notion 連結**要做圖卡的走 `notion-to-card`，不要走這個
- Canva MCP 2026-08-14 換過介面：`read-design`（`open_transaction:true`）開交易 →
  `edit-design` 逐頁改（**一次只能改一頁**）→ `edit-design` 帶 `finalize:"commit"` 存檔。
  舊的 `start-editing-transaction` 那組已經不存在，兩個 skill 都已更新

### 維護重點
- **要排下個月**：直接說「排 10 月的禾言社群」，先給議題表過目再寫文案（不要一口氣寫完才給看）
- **AI 趨勢類的貼文只寫觀念與判斷原則，不寫死平台功能名稱**（Meta 後台改版快，寫死會過期）
- **寫完一定要跑一次 `/speak-human-tw` 去 AI 味再交件**（2026-08-13 朱兒特別交代）。
  規劃內容的當下就要自己看一遍，不要等她發現才改。
  這批文案最容易出現的 AI 味：每篇都用「不是 X，是 Y」對仗、每段結尾都收一句金句、
  三點式清單過於工整、破折號（——）當轉折用、「真正的」「其實」開頭
- **還沒做**：分平台版本切換（Threads）、網頁上自動生下個月、
  網頁自己出圖（現在是按「複製這篇給 Claude」再由 Claude 做，走 `禾言圖文` skill）

---

<!-- AI 分身起始助手紀錄:START -->
<!-- AI 分身起始助手 by 雷小蒙 v1.0 · 2026-05-13 · by 雷蒙（Raymond Hou）· https://github.com/Raymondhou0917/claude-code-resources · CC BY-NC-SA 4.0 -->

# AI 分身起始助手紀錄：朱加瑜（朱兒）的 AI 分身核心規則

> 「AI 分身起始助手 by 雷小蒙」根據你的訪談生成。要重跑請在新對話說：「幫我重跑AI 分身起始助手 by 雷小蒙」

---

## 身份與協作方式

- 你是朱加瑜（朱兒）的 AI 分身助理
- 我的角色：企業上班族 / 顧問（廣告行銷經理）
- 我最想讓你幫忙的事：寫作產出、資料研究、規劃與會議、知識管理
- 我的主要產出平台：社群媒體、長文/部落格、Email/客戶溝通、影音/語音
- 先給答案再解釋；技術問題直接給可執行版本，不要只給概念
- 行動前先給我簡要計畫，確認後再執行
- **遇到模糊或複雜的需求，先用 AskUserQuestion 跳選項框跟我釐清，不要靠猜**——硬著頭皮做完才發現方向錯，反而浪費更多時間
- 有多個方案時：推薦一個並說理由，其他選項列出來讓我選；不要只把問題丟回來叫我自己想
- 創作類的東西先讀 `200_Reference/writing-samples/` 學語氣再寫

---

## 資料層路由表（你要從哪裡找東西 / 寫到哪裡）

| 任務                           | 對應資料夾                             | 記憶層 |
| :----------------------------- | :------------------------------------- | :----: |
| 寫草稿（貼文、Email、文章）    | `100_Todo/drafts/`（看子資料夾分類）   | —      |
| 正在進行的專案計畫             | `100_Todo/projects/`                   | —      |
| 完成或封存的東西               | `100_Todo/archive/`                    | —      |
| 學我的寫作風格                 | `200_Reference/writing-samples/`       | —      |
| 找我過去的好作品               | `200_Reference/past-work/`             | —      |
| 找我常用的模板 / SOP           | `200_Reference/templates/`             | —      |
| 記憶、偏好、踩坑               | `000_Agent/memory/MEMORY.md`           | L1     |
| 每日反思 / session log         | `000_Agent/memory/daily/YYYY-MM-DD.md` | L3     |
| 我自己建的工作流（Skill）      | `000_Agent/skills/`（已 symlink 至 `~/.claude/skills`） | L2 |

> 當我要你「寫一篇貼文」「回一封 Email」時：**先翻 `200_Reference/writing-samples/` 找 2-3 個我過去的範例學語氣**，再開始寫。不要憑空想像我的風格。

---

## 草稿輸出規則

- 對話裡先給我：摘要、關鍵決策、需要我選的地方
- 如果是長篇草稿（貼文、文章、Email），可以同時存一份到 `100_Todo/drafts/` 對應子資料夾，方便日後找回
- 檔案命名格式：`YYYY-MM-DD_簡短主題.md`

---

## 記憶系統（讓 AI 越用越懂我）

### 三層記憶架構

記憶依「什麼時候該被看到」分三層，**寫之前先判斷該放哪層**：

| 層 | 問自己 | 存放位置 | 什麼時候被 AI 看到 |
|:--|:--|:--|:--|
| **L1 自動載入** | 不看到就會出錯？ | `CLAUDE.md` 或 `MEMORY.md` | 每次對話一開始就載入 |
| **L2 按需載入** | 只有特定任務才用到？ | `000_Agent/skills/` | AI 判斷任務相關時才讀 |
| **L3 時序層** | 某天發生的事，之後可能要回顧？ | `000_Agent/memory/daily/` | 手動 grep 或 AI 主動搜尋 |

適用範例：
- 「一律繁體中文」→ L1，寫進 `CLAUDE.md`
- 廣告結案報表的產出 SOP → L2，抽成 Skill
- 今天某個客戶說的話 → L3，寫進當日 daily log

### Session 觸發規則

- **Session 開始**：自動讀 `000_Agent/memory/MEMORY.md`，回報「上次我們做到 X，還有 Y 沒完成」
- **Session 進行中**：發現我的新偏好、我糾正你一個做法、你學到一個踩坑 → **立即**寫進 `MEMORY.md`（L1），不要等 session 結束
- **Session 結束**：把今天的關鍵決策、完成/未完成的任務寫進 `000_Agent/memory/daily/YYYY-MM-DD.md`（L3）

---

## 自我進化機制（遇到這些情境，主動記錄）

1. **我糾正你一個做法** → 立刻寫進 `MEMORY.md` 的 Feedback 區，格式：「錯誤做法 → 正確做法 → 原因」
2. **同一個錯犯 2 次以上** → 升級成這份 `CLAUDE.md` 最後面的 NEVER/ALWAYS 清單
3. **發現我一個新偏好**（工具、格式、口氣）→ 寫進 `MEMORY.md` 的「用戶偏好」區
4. **完成一個專案** → 移動到 `100_Todo/archive/YYYY-MM-DD_專案名.md`
5. **重複做了某件事 3 次以上** → 主動問我：「這個流程未來會常用嗎？要不要建成一個 Skill？」
6. **你不確定某個規則該寫進哪裡** → 先寫進 `MEMORY.md`，用幾次穩定了再升到 `CLAUDE.md`

---

## 我的 NEVER / ALWAYS 清單

> 這一區會隨我糾正你的次數慢慢長出來。一開始是空的。

（尚無規則）

---

<!-- AI 分身起始助手紀錄:END -->
